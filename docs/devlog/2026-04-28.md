# 2026-04-28 Devlog

## 14:32 EDT - Add gmailwiz to project whitelist

### Summary
Added `gmailwiz` (`~/gmailwiz`) as a fourth recognized project in the Drafts→DO→Mac td-sync pipeline. Routine two-place edit using the workflow established when giftlist was added.

### What Was Done
- Verified `~/gmailwiz` exists with a populated `.todos/` (td DB).
- Added `gmailwiz` to `PROJECT_MAP` in `td_do_puller.py` (Mac puller).
- Added `gmailwiz` to the project whitelist set in `do-server/server.py` (canonical droplet copy) and updated the error message text.
- Backed up the live `/opt/td-capture/server.py` on the droplet, `scp`'d the new version, restarted `td-capture.service`, confirmed `is-active` and a bogus-project probe returns the new error string.
- Updated `README.md` and `DO_DRAFTS_SETUP.md` examples + allowed-project lists.

### Files Modified
- `td_do_puller.py` — added `'gmailwiz': '/Users/gaylonvorwaller/gmailwiz'`
- `do-server/server.py` — whitelist set + error message
- `README.md` — example draft + DO server section's allowed list
- `DO_DRAFTS_SETUP.md` — example draft + allowed list
- On droplet: `/opt/td-capture/server.py` deployed; backup at `server.py.bak.20260428-182919`

### Key Technical Insight
Nothing new — the workflow from the giftlist add held up cleanly:
1. Edit `PROJECT_MAP` in `td_do_puller.py`.
2. Edit the whitelist set in `do-server/server.py`.
3. `scp` to droplet, `systemctl restart td-capture.service`, smoke-test `/health` and a bogus-project probe.
4. Update README + DO_DRAFTS_SETUP example/allowed lists.

The bogus-project probe is the cleanest verification — its error message echoes the current whitelist, so a single 400 response confirms the deploy reflects the source.

### Next Steps
- Send a `gmailwiz@...` draft from the phone to verify end-to-end (queue enqueue → Mac puller pulls → `td create` lands in `~/gmailwiz/.todos/issues.db`).
- Optional: `do-server/deploy.sh` to script the scp+restart+smoke-test (would also reduce the chance of forgetting the backup step).
